#include <stdio.h>

int main() {
    int num, reverse = 0;
    printf("Enter a positive integer:");
    scanf("%d", &num);
    printf("Digits in reverse order:");
    while (num !=0) {
        printf("%d",num % 10);
        num/=10;
    }
    printf("\n");
    return 0;
}
